language: android
jdk: oraclejdk8

android:
  components:
    - build-tools-26.0.2
    - android-26

before_install:
  - chmod +x gradlew
  - chmod +x gradle/wrapper/gradle-wrapper.jar

script:
  - ./gradlew clean build connectedCheck
