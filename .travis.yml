language: android
jdk: oraclejdk8

android:
  components:
    - build-tools-26.1.1
    - android-28

before_install:
  - chmod +x gradlew
  - chmod +x gradle/wrapper/gradle-wrapper.jar

script:
  - ./gradlew clean build connectedCheck
